Django==1.7
mongoengine==0.8.7
djangorestframework==2.3.13
django-oauth-toolkit==0.8.0
django-rest-framework-mongoengine==1.3
django-rest-swagger==0.1.14
xlrd==0.9.3
xmltodict==0.8.7
lxml==3.4.1
python-dateutil==2.2
requests==2.3.0

# pymongo needs to be downgraded to 2.8 to be supported by mongoengine
pymongo==2.8

# postgreSQL can be used in production instead of SQLite.
# SQLite is used by default.
# Uncomment the following line if using postgreSQL
# psycopg2
